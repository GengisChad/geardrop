import "server-only";import { createClient } from "@supabase/supabase-js";import { readPublicSupabaseEnv } from "@/lib/supabase/env";import type { Database } from "@/lib/supabase/database.types";import { cacheStorefrontRead,STOREFRONT_CACHE_TAGS } from "./cache";
export async function getPublicStoreSettings(){return cacheStorefrontRead(["settings"],[STOREFRONT_CACHE_TAGS.settings],async()=>{const{url,publishableKey}=readPublicSupabaseEnv();const client=createClient<Database>(url,publishableKey,{auth:{autoRefreshToken:false,detectSessionInUrl:false,persistSession:false}});const result=await client.from("site_settings").select("accept_orders,currency,max_quantity_per_line,store_name,support_email,support_phone,maintenance_mode,maintenance_message,default_seo_title,default_seo_description,default_og_image_url,instagram_url,facebook_url,tiktok_url,youtube_url").eq("singleton",true).single();if(result.error)throw new Error("Impostazioni vetrina non disponibili");return result.data;});}
