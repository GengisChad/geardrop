import "server-only";import { createClient } from "@supabase/supabase-js";import { draftMode } from "next/headers";import { requireStaffRole,requireUser } from "@/lib/auth/guards";import { getContentPage,getFooter,getNavigation,listHomepageSections } from "@/lib/content/repository";import { readPublicSupabaseEnv } from "@/lib/supabase/env";import { createSupabaseServerClient } from "@/lib/supabase/server";import type { Database } from "@/lib/supabase/database.types";import { cacheStorefrontRead,STOREFRONT_CACHE_TAGS } from "./cache";
function createPublicClient(){const{url,publishableKey}=readPublicSupabaseEnv();return createClient<Database>(url,publishableKey,{auth:{autoRefreshToken:false,detectSessionInUrl:false,persistSession:false}});}
async function previewClient(){const client=await createSupabaseServerClient();await requireUser(client);await requireStaffRole(client,["owner","admin","editor"]);return client;}
async function previewEnabled(){return(await draftMode()).isEnabled;}
export async function getStorefrontHomepage(){if(await previewEnabled())return listHomepageSections(await previewClient(),{includeDrafts:true});return cacheStorefrontRead(["homepage"],[STOREFRONT_CACHE_TAGS.homepage,STOREFRONT_CACHE_TAGS.products,STOREFRONT_CACHE_TAGS.categories],()=>listHomepageSections(createPublicClient()));}
export async function getStorefrontPage(slug:string){if(await previewEnabled())return getContentPage(await previewClient(),slug,{includeDrafts:true});return cacheStorefrontRead(["page",slug],[STOREFRONT_CACHE_TAGS.pages],()=>getContentPage(createPublicClient(),slug));}
export async function getStorefrontChrome(){if(await previewEnabled()){const client=await previewClient();const[desktop,mobile,footer]=await Promise.all([getNavigation(client,"desktop",{includeDrafts:true}),getNavigation(client,"mobile",{includeDrafts:true}),getFooter(client,{includeDrafts:true})]);return{desktop,mobile,footer};}return cacheStorefrontRead(["chrome"],[STOREFRONT_CACHE_TAGS.navigation,STOREFRONT_CACHE_TAGS.footer],async()=>{const client=createPublicClient();const[desktop,mobile,footer]=await Promise.all([getNavigation(client,"desktop"),getNavigation(client,"mobile"),getFooter(client)]);return{desktop,mobile,footer};});}
