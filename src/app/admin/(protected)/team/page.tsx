import Link from "next/link";import { redirect } from "next/navigation";import { InviteStaffForm } from "@/components/admin/team/team-forms";import styles from "@/components/admin/team/team.module.css";import { requireAdminAccess } from "@/lib/admin/access";import { listStaff } from "@/lib/admin/team-repository";import { createSupabaseServerClient } from "@/lib/supabase/server";
export const dynamic="force-dynamic";export const fetchCache="force-no-store";
export default async function TeamPage(){const client=await createSupabaseServerClient();const principal=await requireAdminAccess(client);if(principal.role!=="owner")redirect("/admin");const members=await listStaff(client);return <div className={styles.page}><header className={styles.heading}><div><p>Sicurezza / Accessi</p><h1>Team</h1><span>{members.length} profili staff reali</span></div></header><InviteStaffForm/><div className={styles.members}>{members.length?members.map(member=><Link className={styles.member} href={`/admin/team/${member.user_id}`} key={member.user_id}><div><strong>{member.display_name}</strong><span>{member.invite_email??"Email invito non registrata"} · {member.role}</span></div><span className={styles.badge}>{member.active?member.invite_status:"disattivato"}</span></Link>):<p className={styles.empty}>Nessun profilo staff nel database.</p>}</div></div>}
