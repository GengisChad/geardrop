import { draftMode } from "next/headers";import { NextResponse } from "next/server";import { requireStaffRole,requireUser } from "@/lib/auth/guards";import { createSupabaseServerClient } from "@/lib/supabase/server";
function safePath(value:string|null){return value&&value.startsWith("/")&&!value.startsWith("//")&&!value.includes("\\")?value:"/";}
export async function GET(request:Request){const client=await createSupabaseServerClient();try{await requireUser(client);await requireStaffRole(client,["owner","admin","editor"]);}catch{return new Response("Anteprima non autorizzata",{status:401});}const url=new URL(request.url);const draft=await draftMode();if(url.searchParams.get("disable")==="1")draft.disable();else draft.enable();return NextResponse.redirect(new URL(safePath(url.searchParams.get("path")),url.origin));}
