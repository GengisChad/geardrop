# dependencies
node_modules/
.pnp
.pnp.*

# next
/.next/
/out/
next-env.d.ts

# production
/build

# testing
/coverage
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# python (scripts/build-assets.py)
__pycache__/
*.pyc

# NOTE: assets-source/ and public/ are intentionally tracked.
# assets-source/ holds the originals exactly as supplied (the brief requires keeping
# them); public/ holds the derived binaries, which are produced by scripts/build-assets.py
# and committed so a clone builds without Python installed.
