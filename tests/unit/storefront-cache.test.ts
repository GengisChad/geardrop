import { readFileSync } from "node:fs";import { join } from "node:path";import { describe,expect,it } from "vitest";
describe("storefront cache",()=>{it("uses tagged cache loaders without a module-global Supabase client",()=>{const source=readFileSync(join(process.cwd(),"src/lib/storefront/cache.ts"),"utf8");expect(source).toContain("unstable_cache");for(const tag of ["products","categories","homepage","navigation","footer","pages","settings","shipping","promotions"])expect(source).toContain(`\"${tag}\"`);expect(source).not.toMatch(/^const client\s*=/m);});it("uses stable safe errors without leaking environment values",()=>{const source=readFileSync(join(process.cwd(),"src/lib/commerce/provider.ts"),"utf8");expect(source).toContain("Provider commerce non supportato");expect(source).not.toContain("JSON.stringify(process.env)");});});
